import { PaginationContextState } from './state';
export declare const contextSelectors: {
    currentPageIds: ({ pages, currentPage, }: PaginationContextState) => (string | number)[];
    allIds: ({ pages }: PaginationContextState) => (string | number)[];
    allPageIds: ({ pages }: PaginationContextState) => (string | number)[][];
    nextPageLoaded: ({ pages, currentPage }: PaginationContextState) => boolean;
    nextPageLoading: ({ pages, currentPage, loadingNewPage, }: PaginationContextState) => boolean;
    loadingNewPage: ({ loadingNewPage }: PaginationContextState) => boolean;
    pageNumber: ({ currentPage }: PaginationContextState) => number;
    done: ({ done }: PaginationContextState) => boolean;
    finalPageIndex: ({ done, pages }: PaginationContextState) => number;
    onFirstPage: ({ currentPage }: PaginationContextState) => boolean;
    onFinalPage: (state: PaginationContextState) => boolean;
};
export declare type ContextSelectors = typeof contextSelectors;
