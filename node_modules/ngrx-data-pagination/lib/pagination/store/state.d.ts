import { EntityId } from '../entity';
export interface PaginationContextState {
    pages: EntityId[][];
    loadingNewPage: boolean;
    currentPage: number;
    done: boolean;
    progressionCancelled: boolean;
}
export interface PaginationState {
    contexts: {
        [contextId: string]: PaginationContextState;
    };
}
export declare const defaultPaginationContextState: PaginationContextState;
export declare const defaultPaginationState: PaginationState;
