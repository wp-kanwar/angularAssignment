export declare type EntityId = string | number;
export declare type EntityMap<Entity> = {
    [id: number]: Entity;
} | {
    [id: string]: Entity;
};
export interface AnyEntity {
    id: EntityId;
}
